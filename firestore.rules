rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Orders — anyone can create, only functions can read/update
    match /orders/{orderId} {
      allow create: if true;
      allow read, update: if false; // via Functions only
    }
  }
}
